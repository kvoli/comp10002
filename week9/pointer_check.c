#include <stdio.h>

struct Book {
  char* author,*title, *publisher;
  int num_pages, date;
};

// unsigned char

void book_func(struct Book b)

int main(int argc, char const* argv[])
{
  struct Book book;
  book.author = "Austen";
  return 0;
}
